define([ 'app/js/dspace' ], 
function( DSpace ) {
    console.log("loading?");
    var world = new DSpace();
    world.init( );
 });

///**
// * BIG BANG!
// */
//$.domReady(function () {
//
//  var world = new DSpace();
//  world.init(config);
//
//});

